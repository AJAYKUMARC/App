@model App.ViewModels.TransactionViewModel; 
    @{
        ViewData["Title"] = "TransactionView";
        Layout = "~/Views/Shared/_LayoutDash.cshtml";
    }

    @await Html.PartialAsync("_InputView", Model.Input)
    @if (Model.GridView != null)
    {
        @await Html.PartialAsync("_GridView", Model.GridView)
    }
